name: UltraTV-Final-Success

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: 'رابط الفيديو'
        required: true
        default: 'http://ibo.lynxiptv.com/334188914694/VzDd0nBIgq/66384'

jobs:
  streaming_job:
    runs-on: ubuntu-latest
    steps:
      - name: Start Streaming with Docker
        run: |
          # تشغيل حاوية مخصصة للبث مع تحديث شهادات SSL فوراً
          docker run --rm jrottenberg/ffmpeg:4.4-ubuntu \
          -re -i "${{ github.event.inputs.video_url }}" \
          -user_agent "Mozilla/5.0" \
          -c:v libx264 -preset superfast -b:v 2500k -maxrate 2500k -bufsize 5000k \
          -pix_fmt yuv420p -g 50 -c:a aac -b:a 128k -ar 44100 \
          -f flv "rtmps://live-api-s.facebook.com:443/rtmp/FB-122183779916765411-0-Ab7AHzV47Gn0sQotY5C_kIMZ"
